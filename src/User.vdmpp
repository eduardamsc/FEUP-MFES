class User
	instance variables
		private m_username: seq of char;
		private m_password: seq of char;
	
	operations
		public User(username: seq of char, password: seq of char) user: User == (
			m_username := username;
			m_password := password;
			return self;
		)
			
end User